#version: '2'
#services:
#  visualization-app:
#    image: dashboards_visualization-app
#    ports:
#      - "5000:5002"
#    volumes:
#      - .:/visualization

version: '3.1'
services:
  db:
     image: mysql
     restart: always
     environment:
       MYSQL_ROOT_PASSWORD: root
       MYSQL_DATABASE: test_db
     ports:
       - "3307:3306"
networks:
  default:
    external: true
    name: twitter-streaming_default